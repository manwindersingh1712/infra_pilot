# Default server — catches requests that don't match any service subdomain
server {
    listen 80 default_server;
    server_name _;

    location / {
        return 404 '{"error":"not_found"}\n';
        default_type application/json;
    }
}
